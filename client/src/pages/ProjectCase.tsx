// Zurc — case detail: uma única estrutura renderiza qualquer projeto do catálogo.
import { ArrowLeft, ArrowUpRight } from "lucide-react";
import { Link, useRoute } from "wouter";
import { getProjectBySlug } from "../data/projects";

export default function ProjectCase() { const [, params] = useRoute("/projetos/:slug"); const project = params ? getProjectBySlug(params.slug) : undefined; if (!project) return <div className="inner-page not-found"><div className="container"><div className="eyebrow"><i /> 404 / PROJETO</div><h1>Este estudo ainda<br /><em>não existe.</em></h1><Link className="text-link" href="/projetos"><ArrowLeft size={16} /> Voltar para projetos</Link></div></div>; return <div className="inner-page"><section className="case-hero"><div className="container"><Link className="back-link" href="/projetos"><ArrowLeft size={15} /> Todos os projetos</Link><div className="eyebrow"><i /> {project.label}</div><h1>{project.title}<br /><em>{project.category}.</em></h1><p>{project.description}</p></div></section><div className="container case-image"><img src={project.image} alt={`Visual conceitual ampliado de ${project.title}`} /></div><section className="case-info"><div className="container case-info-grid"><div><span className="detail-index">{project.index}</span><h2>Uma hipótese<br /><em>em sistema.</em></h2></div><div><p className="case-lead">Este é um estudo conceitual da Zurc. Ele explora como clareza, estrutura e tecnologia podem se encontrar em uma experiência digital.</p><div className="case-meta"><div><span>TECNOLOGIAS</span><p>{project.technologies.join(" · ")}</p></div><div><span>STATUS</span><p>{project.result}</p></div></div></div></div></section><section className="inner-cta"><div className="container"><h2>Quer investigar<br /><em>um problema real?</em></h2><Link className="button button-red" href="/contato">Abrir conversa <ArrowUpRight size={16} /></Link></div></section></div>; }
