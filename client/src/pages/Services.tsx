// Zurc — serviços: cada bloco é derivado do catálogo central e pode crescer sem duplicar markup.
import { ArrowUpRight, Check } from "lucide-react";
import { Link } from "wouter";
import { services } from "../data/services";

export default function Services() { return <div className="inner-page"><section className="page-hero"><div className="container"><div className="eyebrow"><i /> 01 / SERVIÇOS</div><h1>Ferramentas certas<br /><em>para problemas reais.</em></h1><p>Não começamos pela tecnologia. Começamos pelo que precisa mudar.</p></div></section><section className="service-detail-list">{services.map((service) => <article className="service-detail" id={service.slug} key={service.id}><div className="container detail-grid"><div><span className="detail-index">{service.number}</span><h2>{service.title}</h2><p className="detail-lead">{service.description}</p><Link className="text-link" href="/contato">Falar sobre este serviço <ArrowUpRight size={16} /></Link></div><div className="detail-columns"><div><h3>Resolve</h3>{service.problems.map((item) => <p key={item} className="list-line"><span>—</span>{item}</p>)}</div><div><h3>Entrega</h3>{service.benefits.map((item) => <p key={item} className="list-line"><Check size={14} />{item}</p>)}</div><div><h3>Processo</h3>{service.process.map((item, index) => <p key={item} className="list-line"><span>0{index + 1}</span>{item}</p>)}</div></div></div></article>)}</section><section className="inner-cta"><div className="container"><h2>O próximo problema<br /><em>começa aqui.</em></h2><Link className="button button-red" href="/contato">Abrir conversa <ArrowUpRight size={16} /></Link></div></section></div>; }
