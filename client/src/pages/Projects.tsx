// Zurc — portfólio: catálogo de projetos conceituais sem prova social fabricada.
import { ArrowUpRight } from "lucide-react";
import { Link } from "wouter";
import { projects } from "../data/projects";

export default function Projects() { return <div className="inner-page"><section className="page-hero"><div className="container"><div className="eyebrow"><i /> 02 / PROJETOS</div><h1>Ideias que ganham<br /><em>forma e sistema.</em></h1><p>Uma seleção de explorações em interface, automação e produtos digitais.</p></div></section><section className="portfolio-list"><div className="container">{projects.map((project) => <Link className="portfolio-row" href={`/projetos/${project.slug}`} key={project.id}><div className="portfolio-image"><img src={project.image} alt={`Visual conceitual de ${project.title}`} loading="lazy" /></div><div className="portfolio-copy"><span>{project.label}</span><h2>{project.title}</h2><p>{project.description}</p><div className="tag-list">{project.technologies.map((tag) => <span key={tag}>{tag}</span>)}</div></div><ArrowUpRight className="portfolio-arrow" size={22} /></Link>)}</div></section><section className="inner-cta"><div className="container"><h2>Seu projeto pode ser<br /><em>o próximo estudo.</em></h2><Link className="button button-red" href="/contato">Conversar sobre projeto <ArrowUpRight size={16} /></Link></div></section></div>; }
